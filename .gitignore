# BMO credentials — NEVER commit
.env

# Raw CSV exports — internal use only
raw_csv/
*.csv

# Local dev
__pycache__/
*.pyc
.DS_Store
logs/
.DS_Store

# Interior scan binaries — large, host externally or use LFS
scans/*.glb
scans/*.gltf
scans/*.bin
scans/*.ply
scans/*.pcd
scans/*.splat
scans/*.e57
scans/*.las
!scans/.gitkeep
!scans/README.md
